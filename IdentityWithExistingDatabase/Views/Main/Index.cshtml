@model IdentityWithExistingDatabase.Models.DAL.Entities.User
@{
    ViewBag.Title = "Index";
}

<div class="jumbotron">
    <h1 class="text-info">Welcome back, @Model.Name!!!</h1>
    <h3 class="text-success">Roles: @string.Join(",", Model.Roles.Select(role => role.Name))</h3>
</div>

